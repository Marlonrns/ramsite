<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= character.name %></title>
</head>
<body>
  <h1><%= character.name %></h1>
  <img src="<%= character.image %>" alt="<%= character.name %>">
  <p>Status: <%= character.status %></p>
  <p>Species: <%= character.species %></p>

  <nav>
    <% if (prevCharacter) { %>
      <a href="<%= prevCharacter.id %>_<%= prevCharacter.name.replace(/[^a-z0-9]/gi, '-').toLowerCase() %>.html">« Previous</a>
    <% } %>
    <% if (nextCharacter) { %>
      <a href="<%= nextCharacter.id %>_<%= nextCharacter.name.replace(/[^a-z0-9]/gi, '-').toLowerCase() %>.html">Next »</a>
    <% } %>
  </nav>

  <br>
  <a href="index.html">Back to list</a>
</body>
</html>
